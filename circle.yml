dependencies:
  cache_directories:
    - "~/.stack"
  pre:
    - mkdir tmp
    - cd tmp
    - wget https://github.com/commercialhaskell/stack/releases/download/v1.0.2/stack-1.0.2-linux-x86_64.tar.gz -O stack.tar.gz
    - tar xzf stack.tar.gz && chmod +x /tmp/stack-1.0.2-linux-x86_64
    - sudo mv /tmp/stack-1.0.2-linux-x86_64 /usr/bin/stack-1.0.2-linux-x86_64
  override:
    - stack setup 7.10.3
    - stack build

test:
  pre:
    - cp hs/DevelopmentConfig.hs hs/Config.hs

